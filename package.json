{
  "name": "@xkit/xkit.js",
  "version": "1.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/xkit-co/xkit.js.git"
  },
  "description": "",
  "files": [
    "dist"
  ],
  "main": "dist/index.js",
  "scripts": {
    "dev": "parcel watch index.ts browser.ts",
    "build": "npm test && parcel build --no-cache index.ts browser.ts",
    "test": "tsc --noEmit",
    "prepublishOnly": "npm test",
    "prepare": "rm -rf dist && tsc --emitDeclarationOnly --declaration --declarationDir dist && parcel build --no-cache index.ts",
    "repo": "echo $npm_package_repository_url",
  },
  "author": "Xkit <dev@xkit.co>",
  "license": "MIT",
  "devDependencies": {
    "@babel/core": "~7.10.2",
    "@babel/preset-env": "~7.10.2",
    "@types/phoenix": "^1.4.4",
    "parcel-bundler": "~1.12.4",
    "parcel-plugin-bundle-visualiser": "^1.2.0",
    "typescript": "~3.9.5"
  },
  "dependencies": {
    "event-target-shim": "^5.0.1",
    "phoenix": "^1.5.3"
  }
}
